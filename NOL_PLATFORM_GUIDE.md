# توثيق منصة NoL — ألعاب جماعية فورية بدون حسابات

تم بناء منصة **NoL** وفقاً لأعلى معايير الأمان والتجربة السريعة، حيث تتيح للأصدقاء اللعب المباشر في غرف فورية دون الحاجة إلى إنشاء حسابات، تسجيل بريد إلكتروني، أو حفظ كلمات مرور.

---

## 1. بنية النظام والمعمارية الأساسية

1. **الواجهة الأمامية (Frontend):**
   - مبنية بتقنية React 19 + TypeScript + Tailwind CSS.
   - دعم كامل للغة العربية واتجاه اليمين إلى اليسار (RTL).
   - واجهة ألعاب داكنة احترافية (Modern Gaming Theme) مع مؤثرات زجاجية خفيفة.

2. **قاعدة البيانات والتحكم اللحظي (Database & Realtime):**
   - **PostgreSQL / Supabase Ready:** يتضمن المشروع ملف المخطط الكامل `supabase_schema.sql` ودوال `Edge Functions` تحت مسار `supabase/functions/nol-api/`.
   - **Row Level Security (RLS):** حماية كاملة على الجداول ومنع قراءة الأكواد أو جلسات اللاعبين الحساسة.
   - **Realtime Publication:** تفعيل البث المباشر للغرف، اللاعبين، والأحداث (`rooms`, `room_players`, `game_events`).

3. **هوية اللاعبين المؤقتة (Ephemeral Sessions):**
   - لا توجد أي حسابات دائمة للاعبين.
   - يحصل كل لاعب عند كتابة اسمه على `playerId` فريد و`sessionToken` قوي يتم تشفيره وتخزين الهاش الخاص به فقط.
   - مدة الجلسة الافتراضية 6 ساعات، وتُحذف بعدها تلقائياً.

4. **نظام الغرف وصلاحيات المضيف (Host Control):**
   - كود غرفة غير متسلسل ومولد عشوائياً (مثال: `3TNE64SF`).
   - التحقق من صلاحيات المضيف يتم دائماً من جانب الخادم (Server-side validation) قبل بدء اللعبة أو طرد لاعب أو إغلاق الغرفة.
   - تنظيف تلقائي للغرف الخاملة لأكثر من 30 دقيقة.

5. **أكواد الاشتراك والألعاب الخاصة (Activation Codes):**
   - تخزين أكواد الاشتراك بتشفير SHA-256 Hash.
   - التحقق من صلاحية الكود ومنع إعادة استخدامه بعد تفعيله.
   - منح صلاحية مؤقتة (`session_game_access`) للوصول للعبة الخاصة طوال مدة الاشتراك.

---

## 2. بيانات الدخول للوحة التحكم (Admin)

- **رابط لوحة الإدارة:** `/admin`
- **اسم المستخدم الافتراضي:** `admin`
- **كلمة المرور:** `nol2026vip`
- *ملاحظة:* لوحة الإدارة معزولة بالكامل عن جلسات اللاعبين العاديين.

---

## 3. الألعاب المتاحة حالياً

1. **تحدي المعلومات (Trivia Showdown):** لعبة أسئلة ثقافية وتفاعلية سريعة مع احتساب نقاط فوري.
2. **حروف وكلمات سريعة (Speed Words):** لعبة الحروف والكلمات ضد الوقت.
3. **المندس (The Imposter):** لعبة استنتاج وخداع اجتماعي لكشف اللاعب المندس بين الجميع.
4. **تحدي النخبة (VIP Arena):** لعبة خاصة مغلقة تتطلب كود اشتراك معتمد (كود تجريبي للاختبار: `NOL-7K29-XP41`).

---

## 4. متغيرات البيئة المطلوبة (Environment Variables)

```env
# قاعدة البيانات
DATABASE_URL=mysql://... or postgresql://...

# إعدادات Supabase (عند الربط المباشر مع Supabase Cloud)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOi...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi... (Server-side Only)

# جلسات الإدارة
JWT_SECRET=super_secret_jwt_key
PORT=3000
```

---

## 5. خطوات النشر والتشغيل على Supabase / Node.js

1. **تطبيق قاعدة البيانات في Supabase:**
   - افتح لوحة تحكم Supabase الخاصة بك، وتوجه إلى **SQL Editor**.
   - انسخ محتوى الملف `supabase_schema.sql` ونفذه بالكامل لتفعيل الجداول، وRLS، والـ Realtime Publication.

2. **نشر Edge Functions (اختياري للعمليات الحساسة عبر Supabase CLI):**
   ```bash
   supabase functions deploy nol-api
   ```

3. **تشغيل المشروع:**
   ```bash
   pnpm install
   pnpm build
   pnpm start
   ```
